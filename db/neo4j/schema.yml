# This file is auto-generated from the current state of the database. Instead
# of editing this file, please use the migrations feature of Node to
# incrementally modify your database, and then regenerate this schema definition.
#
# Note that this schema.yml definition is the authoritative source for your
# database schema. If you need to create the application database on another
# system, you should be using neo4j:schema:load, not running all the migrations
# from scratch. The latter is a flawed and unsustainable approach (the more migrations
# you'll amass, the slower it'll run and the greater likelihood for issues).
#
# It's strongly recommended that you check this file into your version control system.

---
:constraints:
# - 'CREATE CONSTRAINT `NODE_Dimension#uuid_key` FOR (n:`Dimension`) REQUIRE (n.`uuid`)
#   IS NODE KEY'
# - 'CREATE CONSTRAINT `NODE_Event#uuid_key` FOR (n:`Event`) REQUIRE (n.`uuid`) IS NODE
#   KEY'
# - 'CREATE CONSTRAINT `NODE_Memo#uuid_key` FOR (n:`Memo`) REQUIRE (n.`uuid`) IS NODE
#   KEY'
# - 'CREATE CONSTRAINT `NODE_Project#uuid_key` FOR (n:`Project`) REQUIRE (n.`uuid`)
#   IS NODE KEY'
# - 'CREATE CONSTRAINT `NODE_Researcher#uuid_key` FOR (n:`Researcher`) REQUIRE (n.`uuid`)
#   IS NODE KEY'
# - 'CREATE CONSTRAINT `NODE_SurveyItem#uuid_key` FOR (n:`SurveyItem`) REQUIRE (n.`uuid`)
#   IS NODE KEY'
# - 'CREATE CONSTRAINT `NODE_SurveyResponse#uuid_key` FOR (n:`SurveyResponse`) REQUIRE
#   (n.`uuid`) IS NODE KEY'
- 'CREATE CONSTRAINT `constraint_dbcee0a4` FOR (n:`ActiveGraph::Migrations::SchemaMigration`)
  REQUIRE (n.`migration_id`) IS UNIQUE'
:indexes:
- CREATE RANGE INDEX `index_2a91af07` FOR (n:`Identity`) ON (n.`name`)
- CREATE RANGE INDEX `index_32e8f300` FOR (n:`Category`) ON (n.`name`)
- CREATE RANGE INDEX `index_4bd64d5` FOR (n:`Code`) ON (n.`name`)
- CREATE RANGE INDEX `index_aabeed83` FOR (n:`Theme`) ON (n.`name`)
:versions:
